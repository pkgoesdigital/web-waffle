name: Nightly Deploy

on:
  schedule:
    - cron: '0 1 * * *'   # 9pm ET (UTC-4 daylight / UTC-5 standard)
  workflow_dispatch:        # allows manual trigger from GitHub UI

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy
        run: curl -X POST ${{ secrets.VERCEL_DEPLOY_HOOK }}
