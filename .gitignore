# dependencies
node_modules/
node_modules

package-lock.json/
package-lock.json

# next.js
.next/
out/

# production
build/
dist/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env files
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# Claude agent harness — runtime data (not config)
# Instinct .yaml files are generated at runtime; only README is committed
.claude/homunculus/instincts/*.yaml
# Session activity logs live at ~/.claude/sessions/ (user-level, outside repo)
